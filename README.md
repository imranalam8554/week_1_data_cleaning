# Week 1 – Data Cleaning and Exploration with Pandas

# Objective: 
Learn Python basics and perform basic data exploration and cleaning using Pandas. Steps: 1.Load a CSV dataset into a Pandas DataFrame. 2.Explore data (head/tail, shape, columns, data types). 3.Handle missing values (identify, fill/drop). 4.Perform basic operations (filter rows, select columns). 5.Remove duplicates. 6.Create a derived column (total_amount = price * quantity). 7.Save the cleaned dataset as a new CSV file. Output: Jupyter Notebook (.ipynb) + cleaned CSV + brief summary.

# Work Completed
Imported the shopping dataset into a Pandas DataFrame for analysis.
Examined the dataset structure, including rows, columns, and data types.
Checked for missing values and handled them appropriately.
Performed basic data manipulation tasks such as selecting specific columns and filtering records based on conditions.
Detected and removed duplicate entries from the dataset.
Added a new column named total_amount using the available data.
Exported the cleaned dataset and saved it as a separate CSV file.

# Tools and Technologies
Python
Pandas
NumPy
Jupyter Notebook